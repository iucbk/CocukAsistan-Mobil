<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:background="@color/design_default_color_primary">

    <Button
        android:id="@+id/btnQuizQuestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="40dp"
        android:background="@drawable/rounded_list_item_item"
        android:drawableStart="@drawable/ic_edit"
        android:drawablePadding="32dp"
        android:elevation="4dp"
        android:lineSpacingExtra="4sp"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:textColor="#594a4e"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Sultan Papağanı" />

    <com.iucbk.cocuk_asistan.util.custom_view.CircleButton
        android:id="@+id/viewQuizState"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignEnd="@+id/btnQuizQuestion"
        android:layout_gravity="top|end"
        android:layout_toEndOf="@+id/btnQuizQuestion"
        android:padding="16dp"
        android:src="@drawable/ic_check"
        app:cb_color="#6fc1a5"
        app:layout_constraintBottom_toTopOf="@+id/btnQuizQuestion"
        app:layout_constraintEnd_toEndOf="@+id/btnQuizQuestion"
        app:layout_constraintStart_toEndOf="@+id/btnQuizQuestion"
        app:layout_constraintTop_toTopOf="@+id/btnQuizQuestion" />
</androidx.constraintlayout.widget.ConstraintLayout>